# 待办事项：智能 PDF 提取逻辑

## 待改进
- [x] **显式项目符号检测**: 目前依赖缩进。添加 regex 检查以 `•`, `-`, `1.` 等开头的行。
- [ ] **表格支持**: 如果需要，集成 `pdfplumber` 或特定逻辑进行表格提取。
- [ ] **单元测试**: 创建模拟 PDF 测试套件以自动验证提取逻辑。
- [ ] **可配置阈值**: 将硬编码值（例如 `5.0` 缩进，`15.0` 垂直距离）移至配置。

## 需要的用户操作
- 重启 MCP 服务器以应用更改。
- 调用 `extract_pdf_content` 时使用 `format="markdown"` 以查看改进后的结构。
